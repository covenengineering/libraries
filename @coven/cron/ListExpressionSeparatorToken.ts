import type { LIST_EXPRESSION_SEPARATOR_TOKEN } from "./tokens.ts";

/**
 * Type to represent the "list expression separator" token (`","`).
 */
export type ListExpressionSeparatorToken =
	typeof LIST_EXPRESSION_SEPARATOR_TOKEN;
