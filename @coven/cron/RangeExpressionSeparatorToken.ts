import type { RANGE_EXPRESSION_SEPARATOR_TOKEN } from "./tokens.ts";

/**
 * Type to represent the "range expression separator" token (`"-"`).
 */
export type RangeExpressionSeparatorToken =
	typeof RANGE_EXPRESSION_SEPARATOR_TOKEN;
